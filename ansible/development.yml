---
- hosts: localhost
  become: yes
  become_method: sudo
  become_user: root
  vars:
    ruby_version: ruby-2.3.1
  roles:
    - role: rvm-setup
      rubies:
        - "{{ ruby_version }}"
    - role: development
